---
description: 项目 TypeScript 开发语言规范
globs: "**/*.ts,**/*.tsx"
alwaysApply: false
---

# TypeScript 开发语言规则

## 编译器与严格性

- 遵循根目录 `tsconfig.json`：`strict`、`noUnusedLocals`、`noUnusedParameters` 已开启，不得关闭。
- 禁止使用 `any`；无法推断时用 `unknown` 并做类型收窄。

```typescript
// ❌ 避免
function handle(data: any) {
  return data.value;
}

// ✅ 推荐
function handle(data: unknown) {
  if (data && typeof data === 'object' && 'value' in data) {
    return (data as { value: unknown }).value;
  }
  throw new Error('Invalid data');
}
```

## 格式与命名

- 缩进：2 空格。
- 文件名：组件用 PascalCase（如 `ChatWidget.tsx`），hooks/composables 用 camelCase 且以 `use` 开头（如 `useOpenClawChat.ts`），其余模块小写或 camelCase。
- 公共 API 从各包 `src/index.ts` 统一导出，不在入口外散落对外导出。

## 类型定义位置

- 协议与客户端相关类型集中在 **core** 的 `packages/core/src/types.ts`；react/vue 仅再导出需要的类型，不重复定义与 core 语义相同的类型。

## 错误处理

- 捕获异常时必须处理或再抛出，禁止空的 `catch`；优先使用 `cause` 保留链路。

```typescript
// ❌ 避免
try {
  await connect();
} catch (e) {}

// ✅ 推荐
try {
  await connect();
} catch (e) {
  logger.error('Connect failed', { error: e });
  throw new ConnectError('Connection failed', { cause: e });
}
```

## 提交前

- 运行 `pnpm lint` 确保符合 ESLint 规则；修改包代码后运行 `pnpm build` 与 `pnpm test` 再提交。
